import { ColumnValuesPipe } from './column-values.pipe';

describe('ColumnValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
